[package]
name = "candle_tensor_augment"
version = "0.1.0"
edition = "2021"

[dependencies]
candle-core = { path = "../../candle-core" }
meval = { version = "0.2", optional = true }

[features]
default = []
expr = ["dep:meval"]
